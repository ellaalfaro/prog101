# Fill in the blanks so the answers are all 2. Use help for unfamiliar functions
# if necessary.
log10(100)
log2(4)
ceiling(1.1)
# floor always rounds down, ceiling always rounds up

# Find the functions for cosine and sine. What are the cosine and sine of 0?
cos(0)
sin(0)
cos(pi)
sin(pi)

# What do you think the following functions do? Just go off the name, don't look
# them up.
read.csv()
# reads a csv file (like google sheets)
boxplot()
# Answer: creates plot a box and whisker graph
startsWith()
# Answer: selecting something that starts with something close
